@{
    ViewData["Title"] = "Exercises";
}

<section class="section exercises-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body p-3 dt-exercises-wrapper"></div>
            </div>
        </div>
    </div>
</section>

@section Scripts
{
    <script>
        $.when(
            $.getScript("/js/pages/exercises/index.js"),
            $.Deferred(function (deferred) {
                $(deferred.resolve);
            })
        ).done(function () {
            exercisesIndexPage.init({
                wrapper: ".dt-exercises-wrapper"
            });
        });
    </script>
}